#include "main.h"

/**
 * main - Entry point; returns the name of the programe
 *
 * @argc: Holds the number of arguments
 *
 * @argv: Holds pointer to the arguments
 *
 * Return: Always 0 at success.
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
