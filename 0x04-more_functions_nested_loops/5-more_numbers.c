#include "main.h"

/**
 * more_numbers - Prints numbers 0 to 14, 10 times
 *
 * Return: Nothing
 */

void more_numbers(void)
{
	int num;
	int ch;

	for (num = 0; num <= 10; num++)
	{
		if (ch = 0; ch < 10; ch++)
			_putchar(ch);

		_putchar('\n');
	}
}
